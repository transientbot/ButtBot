/* This file is named a_definedRanks.js so that it loads before customRanks.js.  No other reason. */

(function ()
{
	function loadIndividualRanks ()
	{
		// Format:  [ "user1" : [ "rank1", "rank2", "rank3", ...], ];
		return [
		];
	};

	$.loadIndividualRanks = loadIndividualRanks;
})();